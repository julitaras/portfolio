# Portfolio

Juli Taras — portfolio personal. React + TypeScript + Vite.

## Desarrollo

```bash
npm install
npm run dev
```

## Deploy

Push a `main` dispara el workflow de GitHub Actions (`.github/workflows/deploy.yml`), que hace build y publica en GitHub Pages: https://julitaras.github.io/portfolio/
